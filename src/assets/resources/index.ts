import {ImageSource} from './type';

export const EMPTY_IMAGE: ImageSource = {
  imageSource: require('./sources/emptyImage.png'),
};

export const RIGHT_ARROW: ImageSource = {
  imageSource: require('./sources/rightArrow.png'),
};
